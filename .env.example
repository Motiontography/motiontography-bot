# Server
PORT=5050

# Admin authentication
ADMIN_TOKEN=replace_with_secure_token

# OpenAI Configuration (required for AI-powered routing)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4o
OPENAI_REASONING_EFFORT=high
OPENAI_TEXT_VERBOSITY=low
OPENAI_MAX_OUTPUT_TOKENS=500

# Notes:
# - If OPENAI_API_KEY is not set, the bot falls back to keyword-based matching
# - Model options: gpt-4o (recommended), gpt-4o-mini (faster/cheaper), gpt-5.2 (when available)
# - Reasoning effort: low, medium, high, xhigh (higher = more "thinking", slower)
# - Text verbosity: low (concise), medium, high (detailed)
